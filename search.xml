<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Arcee：又一个Parser Generator轮子</title>
    <url>/2018/09/02/arcee/</url>
    <content><![CDATA[<p>项目地址：<a href="https://github.com/gcnyin/arcee">https://github.com/gcnyin/arcee</a></p>
<p>为什么要做这样一个东西呢？不是有Antlr吗，Python下不是也有相应的bind吗？人类为什么又要再做一遍已经成熟了的东西呢？</p>
<span id="more"></span>

<p>答案是不爽！</p>
<p>之前刷 EOPL ，想用 Python 改写其中的玩具语言，重写了三四个后，感觉很别扭。教材里自带了一个parser，所以不用考虑解释器前端的东西，但我用Python改写时，由于没有可口的前端，写起来很不爽，每次写完后端，都只能自己用 Python 手敲一遍AST，真的很麻烦，所以我就萌生了自己写一个 parser generator 的想法。</p>
<p>所以，就有 Arcee 。</p>
<p>使用方法：</p>
<p>Install</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">$ pip install Arcee<br></code></pre></td></tr></table></figure>

<p>Example</p>
<p>首先创建grammar文件:</p>
<figure class="highlight mel"><table><tr><td class="code"><pre><code class="hljs mel">KEYWORDS        : let, <span class="hljs-keyword">if</span>, zero, -<br>NUMBER          : \d+(\.\d*)?<br>ASSIGN          : =<br>SUBTRACTION     : -<br>RIGHT_BRACKET   : (<br>COLON           : ,<br>LETF_BRACKET    : )<br>ID              : [A-Za-z]+<br>SKIP            : [ \\t]+<br><br>program : <span class="hljs-keyword">expression</span> ;<br><span class="hljs-keyword">expression</span> : zeroexp<br>    | diffexp<br>    | ifexp<br>    | varexp<br>    | letexp<br>    | constexp<br>    ;<br>constexp : $NUMBER ;<br>diffexp : <span class="hljs-string">&#x27;-&#x27;</span> <span class="hljs-string">&#x27;(&#x27;</span> <span class="hljs-keyword">expression</span> <span class="hljs-string">&#x27;,&#x27;</span> <span class="hljs-keyword">expression</span> <span class="hljs-string">&#x27;)&#x27;</span> ;<br>zeroexp : <span class="hljs-string">&#x27;zero&#x27;</span> <span class="hljs-string">&#x27;(&#x27;</span> <span class="hljs-keyword">expression</span> <span class="hljs-string">&#x27;)&#x27;</span> ;<br>ifexp : <span class="hljs-string">&#x27;if&#x27;</span> <span class="hljs-keyword">expression</span> <span class="hljs-string">&#x27;then&#x27;</span> <span class="hljs-keyword">expression</span> <span class="hljs-string">&#x27;else&#x27;</span> <span class="hljs-keyword">expression</span> ;<br>varexp : $ID ;<br>letexp : <span class="hljs-string">&#x27;let&#x27;</span> $ID <span class="hljs-string">&#x27;=&#x27;</span> <span class="hljs-keyword">expression</span> <span class="hljs-string">&#x27;in&#x27;</span> <span class="hljs-keyword">expression</span> ;<br></code></pre></td></tr></table></figure>

<p>在命令行里执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">$ arcee grammar &gt; result.py<br></code></pre></td></tr></table></figure>

<p>result.py has three parts:</p>
<ol>
<li>Token</li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">from</span> collections <span class="hljs-keyword">import</span> namedtuple<br><br>Token = namedtuple(<span class="hljs-string">&#x27;Token&#x27;</span>, [<span class="hljs-string">&#x27;type&#x27;</span>, <span class="hljs-string">&#x27;value&#x27;</span>, <span class="hljs-string">&#x27;line&#x27;</span>, <span class="hljs-string">&#x27;column&#x27;</span>])<br>Program = namedtuple(<span class="hljs-string">&#x27;Program&#x27;</span>, [<span class="hljs-string">&#x27;expression&#x27;</span>])<br><span class="hljs-comment"># ...</span><br></code></pre></td></tr></table></figure>

<ol start="2">
<li>Lexer</li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> re<br><br><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">tokenize</span>(<span class="hljs-params">code</span>):</span><br>    <span class="hljs-keyword">pass</span> <span class="hljs-comment"># ...</span><br></code></pre></td></tr></table></figure>

<ol start="3">
<li>Parser</li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><code class="hljs python"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Parser</span>:</span><br>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">__init__</span>(<span class="hljs-params">self, token_list</span>):</span><br>        <span class="hljs-keyword">pass</span><br>    <br>    <span class="hljs-comment"># ... </span><br>        <br>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">parse_expression</span>(<span class="hljs-params">self</span>):</span><br>        <span class="hljs-keyword">if</span> xxx:<br>            self.parse_constexp()<br>        <span class="hljs-keyword">elif</span> yyy:<br>            self.parse_diffexp()<br>        <span class="hljs-comment">#...</span><br><br>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">parse_constexp</span>(<span class="hljs-params">self</span>):</span><br>        <span class="hljs-keyword">pass</span><br>        <br>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">parse_diffexp</span>(<span class="hljs-params">self</span>):</span><br>        <span class="hljs-keyword">pass</span><br><br>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">parse_zeroexp</span>(<span class="hljs-params">self</span>):</span><br>        <span class="hljs-keyword">pass</span><br><br>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">parse_ifexp</span>(<span class="hljs-params">self</span>):</span><br>        <span class="hljs-keyword">pass</span><br><br>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">parse_varexp</span>(<span class="hljs-params">self</span>):</span><br>        <span class="hljs-keyword">pass</span><br><br>    <span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">parse_letexp</span>(<span class="hljs-params">self</span>):</span><br>        <span class="hljs-keyword">pass</span><br>You can parse <span class="hljs-built_in">input</span> such <span class="hljs-keyword">as</span>:<br><br><span class="hljs-built_in">input</span> = <span class="hljs-string">&#x27;&#x27;&#x27;let a = 0 in if zero(a) then -(a, 1) else -(a, 2)&#x27;&#x27;&#x27;</span><br><br>tokens = <span class="hljs-built_in">list</span>(tokenize(<span class="hljs-built_in">input</span>))<br><br>parser = Parser(tokens)<br><br>parser.parse_program()<br>result <span class="hljs-keyword">is</span>:<br><br>result = Program(<br>    expression=Expression(<br>        nonterminal=Letexp(<br>            ID=Token(<span class="hljs-built_in">type</span>=<span class="hljs-string">&#x27;ID&#x27;</span>, value=<span class="hljs-string">&#x27;a&#x27;</span>, line=<span class="hljs-number">2</span>, column=<span class="hljs-number">4</span>),<br>            expression1=Expression(<br>                nonterminal=Constexp(<br>                    NUMBER=Token(<span class="hljs-built_in">type</span>=<span class="hljs-string">&#x27;NUMBER&#x27;</span>, value=<span class="hljs-string">&#x27;0&#x27;</span>, line=<span class="hljs-number">2</span>, column=<span class="hljs-number">8</span>))),<br>            expression2=Expression(<br>                nonterminal=Ifexp(<br>                    expression1=Expression(<br>                        nonterminal=Zeroexp(<br>                            expression=Expression(<br>                                nonterminal=Varexp(<br>                                    ID=Token(<span class="hljs-built_in">type</span>=<span class="hljs-string">&#x27;ID&#x27;</span>, value=<span class="hljs-string">&#x27;a&#x27;</span>, line=<span class="hljs-number">2</span>, column=<span class="hljs-number">21</span>))))),<br>                    expression2=Expression(<br>                        nonterminal=Diffexp(<br>                            expression1=Expression(<br>                                nonterminal=Varexp(<br>                                    ID=Token(<span class="hljs-built_in">type</span>=<span class="hljs-string">&#x27;ID&#x27;</span>, value=<span class="hljs-string">&#x27;a&#x27;</span>, line=<span class="hljs-number">2</span>, column=<span class="hljs-number">31</span>))),<br>                            expression2=Expression(<br>                                nonterminal=Constexp(<br>                                    NUMBER=Token(<span class="hljs-built_in">type</span>=<span class="hljs-string">&#x27;NUMBER&#x27;</span>, value=<span class="hljs-string">&#x27;1&#x27;</span>, line=<span class="hljs-number">2</span>,<br>                                                 column=<span class="hljs-number">34</span>))))),<br>                    expression3=Expression(<br>                        nonterminal=Diffexp(<br>                            expression1=Expression(<br>                                nonterminal=Varexp(<br>                                    ID=Token(<span class="hljs-built_in">type</span>=<span class="hljs-string">&#x27;ID&#x27;</span>, value=<span class="hljs-string">&#x27;a&#x27;</span>, line=<span class="hljs-number">2</span>, column=<span class="hljs-number">44</span>))),<br>                            expression2=Expression(<br>                                nonterminal=Constexp(<br>                                    NUMBER=Token(<span class="hljs-built_in">type</span>=<span class="hljs-string">&#x27;NUMBER&#x27;</span>, value=<span class="hljs-string">&#x27;2&#x27;</span>, line=<span class="hljs-number">2</span>,<br>                                                 column=<span class="hljs-number">47</span>))))))))))<br></code></pre></td></tr></table></figure>

<p>这样就获得了AST。</p>
<p>这个轮子目前还有一点小问题，不过自己用的话还是没问题。由于工作缘故，估计是要去学 JavaScript 了，这个东西估计不会再更新了（也许哪天还会的。。。），到时估计就是重写一个 npm 包吧，这个再说。</p>
]]></content>
      <categories>
        <category>compiler</category>
      </categories>
      <tags>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>mini-parser小轮子</title>
    <url>/2019/11/06/mini-parser/</url>
    <content><![CDATA[<p><a href="https://github.com/gcnyin/compiler/blob/master/mini-parser/index.ts">代码链接</a></p>
<span id="more"></span>

<p>demo如下，定义好rule后进行parse。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> rule = AndRule.of(<br>  [<br>    TextRule.of(<span class="hljs-string">&quot;hello&quot;</span>, <span class="hljs-string">&quot;HELLO_RULE&quot;</span>),<br>    OrRule.of(<br>      [TextRule.of(<span class="hljs-string">&quot;, &quot;</span>), TimesRule.of(<span class="hljs-number">3</span>, TextRule.of(<span class="hljs-string">&quot; &quot;</span>, <span class="hljs-string">&quot;SPACE&quot;</span>), <span class="hljs-string">&quot;TIMES&quot;</span>)],<br>      <span class="hljs-string">&quot;NO_NAME&quot;</span><br>    ),<br>    TextRule.of(<span class="hljs-string">&quot;world&quot;</span>, <span class="hljs-string">&quot;WORLD&quot;</span>),<br>    OneOrMoreRule.of(TextRule.of(<span class="hljs-string">&quot;!&quot;</span>), <span class="hljs-string">&quot;SAMPLE&quot;</span>)<br>  ],<br>  <span class="hljs-string">&quot;HELLO_WORLD&quot;</span><br>);<br><br><span class="hljs-built_in">console</span>.log(<span class="hljs-built_in">JSON</span>.stringify(rule.accept(<span class="hljs-string">&quot;hello   world!!!&quot;</span>)));<br></code></pre></td></tr></table></figure>

<p>结果为：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json">&#123;<br>  <span class="hljs-attr">&quot;contain&quot;</span>: <span class="hljs-literal">true</span>,<br>  <span class="hljs-attr">&quot;group&quot;</span>: &#123;<br>    <span class="hljs-attr">&quot;groups&quot;</span>: [<br>      &#123; <span class="hljs-attr">&quot;text&quot;</span>: <span class="hljs-string">&quot;hello&quot;</span>, <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-string">&quot;HELLO_RULE&quot;</span> &#125;,<br>      &#123;<br>        <span class="hljs-attr">&quot;groups&quot;</span>: [<br>          &#123;<br>            <span class="hljs-attr">&quot;groups&quot;</span>: [<br>              &#123; <span class="hljs-attr">&quot;text&quot;</span>: <span class="hljs-string">&quot; &quot;</span>, <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-string">&quot;SPACE&quot;</span> &#125;,<br>              &#123; <span class="hljs-attr">&quot;text&quot;</span>: <span class="hljs-string">&quot; &quot;</span>, <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-string">&quot;SPACE&quot;</span> &#125;,<br>              &#123; <span class="hljs-attr">&quot;text&quot;</span>: <span class="hljs-string">&quot; &quot;</span>, <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-string">&quot;SPACE&quot;</span> &#125;<br>            ],<br>            <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-string">&quot;TIMES&quot;</span><br>          &#125;<br>        ],<br>        <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-string">&quot;NO_NAME&quot;</span><br>      &#125;,<br>      &#123; <span class="hljs-attr">&quot;text&quot;</span>: <span class="hljs-string">&quot;world&quot;</span>, <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-string">&quot;WORLD&quot;</span> &#125;,<br>      &#123;<br>        <span class="hljs-attr">&quot;groups&quot;</span>: [<br>          &#123; <span class="hljs-attr">&quot;text&quot;</span>: <span class="hljs-string">&quot;!&quot;</span>, <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-literal">null</span> &#125;,<br>          &#123; <span class="hljs-attr">&quot;text&quot;</span>: <span class="hljs-string">&quot;!&quot;</span>, <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-literal">null</span> &#125;,<br>          &#123; <span class="hljs-attr">&quot;text&quot;</span>: <span class="hljs-string">&quot;!&quot;</span>, <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-literal">null</span> &#125;<br>        ],<br>        <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-string">&quot;SAMPLE&quot;</span><br>      &#125;<br>    ],<br>    <span class="hljs-attr">&quot;name&quot;</span>: <span class="hljs-string">&quot;HELLO_WORLD&quot;</span><br>  &#125;<br>&#125;<br>  <br></code></pre></td></tr></table></figure>

<p>当然，这离真正可用还有很远：</p>
<p><img src="/images/honghongjiedejiaodao.jpg" alt="honghongjie"></p>
]]></content>
      <categories>
        <category>parser</category>
      </categories>
      <tags>
        <tag>typescript</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM if&lt;cond&gt;指令笔记</title>
    <url>/2019/09/13/ifcond-note/</url>
    <content><![CDATA[<figure class="highlight apache"><table><tr><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">format</span>: if&lt;cond&gt; branchByte<span class="hljs-number">1</span> branchByte<span class="hljs-number">2</span><br></code></pre></td></tr></table></figure>

<span id="more"></span>

<p>从栈中弹出一个值，和0进行比较，根据指令的不同，有不同的比较方法得出一个值，如果为假，则顺序执行后面的指令。那为真的呢？：</p>
<p>branchByte1 branchByte2 都是 0x00 - 0xFF 之间的一个 unsigned 值，可以用他俩算出来offset作为true branch的入口，公式为：</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><code class="hljs gcode"><span class="hljs-comment">(branchbyte1 &lt;&lt; 8)</span> | bra<span class="hljs-symbol">nchbyte2</span><br></code></pre></td></tr></table></figure>

<p>例子：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">116:</span> <span class="hljs-string">bipush</span>        <span class="hljs-number">1</span><br><span class="hljs-attr">118:</span> <span class="hljs-string">ifeq</span>          <span class="hljs-number">132</span><br><span class="hljs-attr">121:</span> <span class="hljs-string">getstatic</span>     <span class="hljs-comment">#12      // Field java/lang/System.out:Ljava/io/PrintStream;</span><br><span class="hljs-attr">124:</span> <span class="hljs-string">bipush</span>        <span class="hljs-number">1</span><br><span class="hljs-attr">126:</span> <span class="hljs-string">invokevirtual</span> <span class="hljs-comment">#28      // Method java/io/PrintStream.println:(Z)V</span><br><span class="hljs-attr">129:</span> <span class="hljs-string">goto</span>          <span class="hljs-number">140</span><br><span class="hljs-attr">132:</span> <span class="hljs-string">getstatic</span>     <span class="hljs-comment">#12      // Field java/lang/System.out:Ljava/io/PrintStream;</span><br><span class="hljs-attr">135:</span> <span class="hljs-string">bipush</span>        <span class="hljs-number">0</span><br></code></pre></td></tr></table></figure>

<p>行中ifeq 132的二进制表示为99 00 0E。99代表该指令，00和0E代表两个操作数。</p>
<p>计算偏移量：<code>(0x00 &lt;&lt; 8) | 0x0E</code> 得 14</p>
<p>就是说ifeq开头为118，加上14个偏移量为132，所以132是 true branch 的入口。</p>
<p>问题：</p>
<p>一、为什么需要两个操作数呢？</p>
<p>（一）通过观察 java 编译后的 class 文件，我发现 branchByte1 都是 0x00，完全没有必要为这样一个固定的值设计一个位置呀。</p>
<p>自答：如果 true branch 非常长（true branch 里的指令长度超过了255，那么 branchByte1 是会增长的。</p>
<p>（二）<code>(branchbyte1 &lt;&lt; 8) | branchbyte2</code>的结果还是8位，难道不可以用一个 byte 来表示，为什么需要用两个 byte 计算后得到结果呢？</p>
<p>自答：如上所述，需要两个来处理 condition 很长的情况</p>
]]></content>
      <categories>
        <category>jvm</category>
      </categories>
  </entry>
  <entry>
    <title>slisp：一门简单的JVM上的Lisp方言</title>
    <url>/2019/01/01/slisp/</url>
    <content><![CDATA[<p>之前经常变更学习方向，没有收到很好的学习效果，浪费了不少时间。最近痛定思痛，把方向定为JVM和编译原理，这次真的不改了。本文是学习该方向的阶段性总结。</p>
<span id="more"></span>

<h2 id="一、前言"><a href="#一、前言" class="headerlink" title="一、前言"></a>一、前言</h2><p>之前写过几个解释器，但还没写过编译器。刚好看到知乎Belleve给出的一幅学习路线图，于是决定实现一个lisp方言的编译器。</p>
<p>之所以选择JVM而不是X86作为目标平台，一是JVM平常用的多一些，可以互相印证、互相补充；二是文档和社区资源丰富友好，开发体验较好。</p>
<p>项目地址：<a href="https://github.com/gcnyin/slisp">https://github.com/gcnyin/slisp</a></p>
<p>截止最新的commit 77f126d4，实现的功能有：</p>
<ul>
<li>定义变量</li>
<li>支持字符串、整数和布尔类型</li>
<li>打印以上三种预置类型的值</li>
<li>四则运算</li>
<li>条件判断</li>
</ul>
<h2 id="二、编译和运行"><a href="#二、编译和运行" class="headerlink" title="二、编译和运行"></a>二、编译和运行</h2><p>来一段具体的Slisp程序：</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><code class="hljs lisp">(<span class="hljs-name">define</span> a (<span class="hljs-name">+</span> <span class="hljs-number">1</span> <span class="hljs-number">2</span> <span class="hljs-number">3</span> <span class="hljs-number">4</span>))<br><br>(<span class="hljs-name">println</span> a)<br><br>(<span class="hljs-name">define</span> b (<span class="hljs-name">+</span> a a))<br><br>(<span class="hljs-name">println</span> b)<br><br>(<span class="hljs-name">define</span> a (<span class="hljs-name">+</span> b b))<br><br>(<span class="hljs-name">println</span> a)<br><br>(<span class="hljs-name">println</span> (<span class="hljs-name">+</span> (<span class="hljs-name">+</span> <span class="hljs-number">1</span> <span class="hljs-number">1</span>)<br>            (<span class="hljs-name">-</span> <span class="hljs-number">6</span> <span class="hljs-number">4</span>)<br>            (<span class="hljs-name">*</span> <span class="hljs-number">2</span> <span class="hljs-number">2</span>)<br>            (<span class="hljs-name">/</span> <span class="hljs-number">4</span> <span class="hljs-number">2</span>)))<br><br>(<span class="hljs-name">println</span> <span class="hljs-string">&quot;Hello Slisp!&quot;</span>)<br><br>(<span class="hljs-name">define</span> c <span class="hljs-string">&quot;Hello world!&quot;</span>)<br><br>(<span class="hljs-name">println</span> c)<br><br>(<span class="hljs-name">println</span> true)<br><br>(<span class="hljs-name">println</span> false)<br><br>(<span class="hljs-name">define</span> d true)<br><br>(<span class="hljs-name">println</span> d)<br><br>(<span class="hljs-name">if</span> true (<span class="hljs-name">println</span> true) (<span class="hljs-name">println</span> false))<br><br>(<span class="hljs-name">if</span> (<span class="hljs-name">==</span> <span class="hljs-number">1</span> <span class="hljs-number">1</span>) (<span class="hljs-name">println</span> <span class="hljs-string">&quot;1 == 1&quot;</span>) (<span class="hljs-name">println</span> <span class="hljs-string">&quot;1 != 1&quot;</span>))<br></code></pre></td></tr></table></figure>

<p>以上程序出自本项目/Slisp/Hello.slisp。</p>
<p>想要运行必须先打包编译器：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">./gradlew clean build<br></code></pre></td></tr></table></figure>

<p>得到了build/libs/slisp-0.1.0.jar，之后在命令行编译源代码：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">java -jar build/libs/slisp-0.1.0.jar Slisp/Hello.slisp<br></code></pre></td></tr></table></figure>

<p>即可生成Hello.class文件，java Hello运行该文件，输出为：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">20<br>10<br>Hello Slisp!<br>Hello world!<br><span class="hljs-literal">true</span><br><span class="hljs-literal">false</span><br><span class="hljs-literal">true</span><br><span class="hljs-literal">true</span><br>== 1<br></code></pre></td></tr></table></figure>

<h2 id="三、编译器组成"><a href="#三、编译器组成" class="headerlink" title="三、编译器组成"></a>三、编译器组成</h2><p>这个编译器由三部分组成，一是前端部分，二是构建抽象语法树，三是递归下降生成字节码。</p>
<p>前端部分使用了Antlr来构建。Antlr是一个流行的parser generator，可以根据给定的文法，生成相应的parser。因为Slisp本身采用了lisp系的语法，并不复杂，所以很容易写出文法供Antlr使用。</p>
<p>构建抽象语法树使用了visitor模式。由于Antlr本身返回的结果已经是一棵树，所以这部分的工作是，根据每个节点不同的形态创建相应的类和实例。</p>
<p>这里有一些实现上的细节可以优化，比如针对四则运算，可以将这些运算全部用一个类来表示，只更改其中的一个字段以示区别。还有一点是，如果打算只使用一个visitor，那么每个节点类都需要继承同一个接口或父类。</p>
<p>还有，实现了一点简单的类型推导。传统的lisp方言大多是动态语言，不过Slisp是静态的，而且可以在定义变量时推导出变量的类型，不需要开发者手动声明变量的类型。(define a 123)、(define b “Hello”)和(define c true)可以由字面值推导出类型，而(define d (+ 1 (- 2 3))也可以推导出表达式(+ 1 (- 2 3))的类型并以此确定d的类型。</p>
<p>生成字节码部分采用了递归下降来生成。比如对(+ (+ 1 1) (- 6 4) (* 2 2) (/ 4 2))，生成了：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">44: bipush        1<br>46: bipush        1<br>48: iadd<br>49: bipush        6<br>51: bipush        4<br>53: isub<br>54: iadd<br>55: bipush        2<br>57: bipush        2<br>59: imul<br>60: iadd<br>61: bipush        4<br>63: bipush        2<br>65: idiv<br>66: iadd<br></code></pre></td></tr></table></figure>

<p>这段代码是Hello.class文件中的一部分，使用OpenJDK中的javap反汇编器生成。</p>
<p>(+ 1 1)对应44、46和48，先将两个1压入栈中，然后相加，将之前的两个人从栈中弹出，然后将结果压入栈顶，继续执行(- 6 4)。</p>
<p>这里需要注意的是，并不是说执行完这四个运算(+ 1 1) (- 6 4) (* 2 2) (/ 4 2)，然后再计算它们的和。而是在计算完(+ 1 1)和(- 6 4)之后（结果为2和2），立即计算了(+ 2 2)（得到4），然后计算(* 2 2)（得到4），再计算(+ 4 4)，以此类推。过程如下所示：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">(+ (+ 1 1) (- 6 4) (* 2 2) (/ 4 2))<br>(+ 2 (- 6 4) (* 2 2) (/ 4 2))<br>(+ 2 2 (* 2 2) (/ 4 2))<br>(+ 4 (* 2 2) (/ 4 2))<br>(+ 4 4 (/ 4 2))<br>(+ 8 (/ 4 2))<br>(+ 8 2)<br>(10)<br></code></pre></td></tr></table></figure>

<p>为了契合这样的字节码运算方式，后端在创建抽象语法树的时候需要注意“左结合与右结合”的问题。这里采用了右结合的方式，大致结构如下所示：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">(+ (/ 4 2)<br>   (+ (* 2 2)<br>      (+ (- 6 4)<br>         (+ 1 1))))<br></code></pre></td></tr></table></figure>

<p>这样从底层开始生成字节码，每生成一层，就向上传递，继续生成上层节点的字节码。</p>
<p>实际开发中使用了ASM库来辅助生成字节码，只需要手动拼接好类似于bipush 1这样的文本传给ASM中合适的类和方法，最后调用generateBytecode这样的方法即可。</p>
<p>虽然ASM库很方便，但想要生成符合语义的字节码，开发者仍需要阅读JVM规范。JVM规范中定义了各字节码的名称与语义，对照着网络上的众多示例还是很容易理解的。</p>
<h2 id="四、字节码简介"><a href="#四、字节码简介" class="headerlink" title="四、字节码简介"></a>四、字节码简介</h2><p>bipush是指将一个类型为byte扩充为int，然后压到栈上。</p>
<p>iadd是将栈最上面的两个int弹出，然后计算它们的和，将结果压入栈顶。imul、isub和idiv都类似于iadd，不同之处在于将运算符变为了*、-和/。</p>
<p>istore将int保存在局部变量中。</p>
<p>iload从局部变量中取出保存在其中的值。</p>
<p>astore是将对一个Ojbect的引用保存在局部变量中。</p>
<p>alocal是将保存在局部变量中的引用压入栈顶。</p>
<p>ifeq是将栈顶的值与0进行比较，如果相等，进入true branch，否则进行false branch。该指令还会指定一个数字作为false branch入口的地址。</p>
<p>if_icmpne是比较栈上的两个类型为int的值，如果不相等，进入true branch，否则进入false branch。</p>
<p>值得注意的是，诸如if这样的指令并不是单个存在，它们更多的像是一个家庭，比如比较两个int会有许多相似的指令，从JVM规范中抄录一段：</p>
<figure class="highlight smali"><table><tr><td class="code"><pre><code class="hljs smali">• if_icmpeq succeeds<span class="hljs-built_in"> if </span>and only<span class="hljs-built_in"> if </span>value1 = value2<br>• if_icmpne succeeds<span class="hljs-built_in"> if </span>and only<span class="hljs-built_in"> if </span>value1 ≠ value2<br>• if_icmplt succeeds<span class="hljs-built_in"> if </span>and only<span class="hljs-built_in"> if </span>value1 &lt; value2<br>• if_icmple succeeds<span class="hljs-built_in"> if </span>and only<span class="hljs-built_in"> if </span>value1 ≤ value2<br>• if_icmpgt succeeds<span class="hljs-built_in"> if </span>and only<span class="hljs-built_in"> if </span>value1 &gt; value2<br>• if_icmpge succeeds<span class="hljs-built_in"> if </span>and only<span class="hljs-built_in"> if </span>value1 ≥ value2<br></code></pre></td></tr></table></figure>

<p>可以看到if_icmpne只是用来比较两个数相等时的情况，还有其它指令用于比较不等、大于、小于、相等时的情况。像这样相似而略有区别的指令，JVM规范大多将它们的文档合并在一起，并起名为if_icmp<cond>，这里的cond代表每个指令独特的部分。</p>
]]></content>
      <categories>
        <category>compiler</category>
      </categories>
      <tags>
        <tag>jvm</tag>
        <tag>java</tag>
        <tag>lisp</tag>
      </tags>
  </entry>
  <entry>
    <title>AsyncLocalStorage与日志追踪</title>
    <url>/2020/06/25/asynclocalstorage-and-logging/</url>
    <content><![CDATA[<p>最近在思考node.js如何做服务间与服务内部的日志追踪，一个很简单的实现就是在HTTP request header里添加一个字段x-trace-id来标识唯一性，打印日志时添加x-trace-id的值。但如何保存这个状态呢？</p>
<span id="more"></span>

<p>我看到的一些框架和应用会将traceId作为参数传递到应用的各个函数/方法里，调用<code>logger.info(traceId, &quot;XXX&quot;)</code>来实现打印traceId的功能。这样的写法并不优雅，但node.js又不像其他语言框架，比如Spring MVC，可以用过ThreadLocal来保存这个状态，那如何保存这个状态呢？其实node.js已经有了这样一个API：<code>AsyncLocalStorage</code>。</p>
<p><code>AsyncLocalStorage</code>属于async_hooks模块，node.js v14引入，后被反向移植到v12上。下面用一个express应用来说明它的用法。</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-keyword">import</span> &#123; AsyncLocalStorage &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&quot;async_hooks&quot;</span><br><span class="hljs-keyword">import</span> express <span class="hljs-keyword">from</span> <span class="hljs-string">&quot;express&quot;</span><br><span class="hljs-keyword">import</span> &#123; v4 <span class="hljs-keyword">as</span> uuid4 &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&quot;uuid&quot;</span><br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> app = express()<br><span class="hljs-keyword">const</span> t = <span class="hljs-keyword">new</span> AsyncLocalStorage&lt;<span class="hljs-built_in">string</span>&gt;()<br><br>app.use(<span class="hljs-function">(<span class="hljs-params">req, _, next</span>) =&gt;</span> &#123;<br>  <span class="hljs-keyword">const</span> traceId = req.header(<span class="hljs-string">&#x27;x-trace-id&#x27;</span>)<br>  t.run(traceId ?? uuid4(), next)<br>&#125;)<br><br>app.get(<span class="hljs-string">&#x27;/&#x27;</span>, <span class="hljs-keyword">async</span> <span class="hljs-function"><span class="hljs-keyword">function</span> (<span class="hljs-params">_, res</span>) </span>&#123;<br>  <span class="hljs-built_in">console</span>.log(<span class="hljs-string">&#x27;start&#x27;</span>, t.getStore())<br>  <span class="hljs-comment">// 模拟耗时操作</span><br>  <span class="hljs-keyword">await</span> <span class="hljs-keyword">new</span> <span class="hljs-built_in">Promise</span>(<span class="hljs-function">(<span class="hljs-params">resolve</span>) =&gt;</span> &#123;<br>    <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> resolve(), <span class="hljs-number">100</span>)<br>  &#125;)<br>  <span class="hljs-built_in">console</span>.log(<span class="hljs-string">&#x27;end&#x27;</span>, t.getStore())<br>  res.send(<span class="hljs-string">&#x27;Hello World!&#x27;</span>)<br>&#125;)<br><br>app.listen(<span class="hljs-number">3000</span>)<br></code></pre></td></tr></table></figure>

<p>这里我们写了一个middleware，将HTTP Request Header里的信息保存到AsyncLocalStorage实例中，需要打印日志时使用<code>getStore</code>方法获取。</p>
<p>我们期望的行为是：一个请求进来后，保存并打印它的traceId，之后进行了一些异步操作，操作结束后，我们能通过<code>AsyncLocalStorage</code>获取到保存的traceId。下面我们就使用ab命令验证一下。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ab -n 6 -c 2 http://localhost:3000/<br></code></pre></td></tr></table></figure>

<p>从日志中我们看出确实是成功地将状态保存了下来。</p>
<figure class="highlight apache"><table><tr><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">start</span> <span class="hljs-number">1</span>c<span class="hljs-number">9</span>de<span class="hljs-number">33</span>e-<span class="hljs-number">84</span>a<span class="hljs-number">0</span>-<span class="hljs-number">4827</span>-ae<span class="hljs-number">82</span>-eae<span class="hljs-number">9</span>dd<span class="hljs-number">8</span>dec<span class="hljs-number">60</span><br><span class="hljs-attribute">end</span> <span class="hljs-number">1</span>c<span class="hljs-number">9</span>de<span class="hljs-number">33</span>e-<span class="hljs-number">84</span>a<span class="hljs-number">0</span>-<span class="hljs-number">4827</span>-ae<span class="hljs-number">82</span>-eae<span class="hljs-number">9</span>dd<span class="hljs-number">8</span>dec<span class="hljs-number">60</span><br><span class="hljs-attribute">start</span> <span class="hljs-number">9</span>c<span class="hljs-number">1</span>f<span class="hljs-number">6</span>eab-<span class="hljs-number">5</span>c<span class="hljs-number">95</span>-<span class="hljs-number">4</span>a<span class="hljs-number">5</span>f-b<span class="hljs-number">2</span>f<span class="hljs-number">4</span>-<span class="hljs-number">2</span>f<span class="hljs-number">1</span>a<span class="hljs-number">026684</span>f<span class="hljs-number">5</span><br><span class="hljs-attribute">start</span> <span class="hljs-number">61</span>a<span class="hljs-number">32915</span>-<span class="hljs-number">5316</span>-<span class="hljs-number">4066</span>-bfb<span class="hljs-number">0</span>-<span class="hljs-number">382</span>c<span class="hljs-number">41</span>a<span class="hljs-number">2</span>a<span class="hljs-number">1</span>aa<br><span class="hljs-attribute">end</span> <span class="hljs-number">9</span>c<span class="hljs-number">1</span>f<span class="hljs-number">6</span>eab-<span class="hljs-number">5</span>c<span class="hljs-number">95</span>-<span class="hljs-number">4</span>a<span class="hljs-number">5</span>f-b<span class="hljs-number">2</span>f<span class="hljs-number">4</span>-<span class="hljs-number">2</span>f<span class="hljs-number">1</span>a<span class="hljs-number">026684</span>f<span class="hljs-number">5</span><br><span class="hljs-attribute">end</span> <span class="hljs-number">61</span>a<span class="hljs-number">32915</span>-<span class="hljs-number">5316</span>-<span class="hljs-number">4066</span>-bfb<span class="hljs-number">0</span>-<span class="hljs-number">382</span>c<span class="hljs-number">41</span>a<span class="hljs-number">2</span>a<span class="hljs-number">1</span>aa<br><span class="hljs-attribute">start</span> bb<span class="hljs-number">1</span>cb<span class="hljs-number">174</span>-<span class="hljs-number">0</span>f<span class="hljs-number">42</span>-<span class="hljs-number">4</span>dc<span class="hljs-number">6</span>-<span class="hljs-number">9</span>eab-<span class="hljs-number">85</span>ccaef<span class="hljs-number">9</span>d<span class="hljs-number">08</span>c<br><span class="hljs-attribute">start</span> <span class="hljs-number">3</span>ef<span class="hljs-number">04</span>aba-dc<span class="hljs-number">0</span>c-<span class="hljs-number">4</span>c<span class="hljs-number">02</span>-aebf-<span class="hljs-number">850</span>dbf<span class="hljs-number">8</span>c<span class="hljs-number">2</span>e<span class="hljs-number">31</span><br><span class="hljs-attribute">end</span> bb<span class="hljs-number">1</span>cb<span class="hljs-number">174</span>-<span class="hljs-number">0</span>f<span class="hljs-number">42</span>-<span class="hljs-number">4</span>dc<span class="hljs-number">6</span>-<span class="hljs-number">9</span>eab-<span class="hljs-number">85</span>ccaef<span class="hljs-number">9</span>d<span class="hljs-number">08</span>c<br><span class="hljs-attribute">end</span> <span class="hljs-number">3</span>ef<span class="hljs-number">04</span>aba-dc<span class="hljs-number">0</span>c-<span class="hljs-number">4</span>c<span class="hljs-number">02</span>-aebf-<span class="hljs-number">850</span>dbf<span class="hljs-number">8</span>c<span class="hljs-number">2</span>e<span class="hljs-number">31</span><br><span class="hljs-attribute">start</span> a<span class="hljs-number">39</span>d<span class="hljs-number">34</span>be-da<span class="hljs-number">8</span>a-<span class="hljs-number">44</span>f<span class="hljs-number">6</span>-b<span class="hljs-number">3</span>b<span class="hljs-number">4</span>-<span class="hljs-number">3</span>eb<span class="hljs-number">6</span>eb<span class="hljs-number">3501</span>ef<br><span class="hljs-attribute">end</span> a<span class="hljs-number">39</span>d<span class="hljs-number">34</span>be-da<span class="hljs-number">8</span>a-<span class="hljs-number">44</span>f<span class="hljs-number">6</span>-b<span class="hljs-number">3</span>b<span class="hljs-number">4</span>-<span class="hljs-number">3</span>eb<span class="hljs-number">6</span>eb<span class="hljs-number">3501</span>ef<br></code></pre></td></tr></table></figure>

<p>这样的写法比起将logger作为参数在各个函数间传递的做法，可读性、可维护性更强，对业务代码的侵入也更低。</p>
<p>以上是服务内日志追踪的办法。</p>
<p>那如何做服务与服务之间的追踪呢？其实也很简单，将traceId注入到http-client的header中，或者更进一步，重写require函数，调用http/https模块时，将traceId注入。这里就不详细展开。</p>
]]></content>
      <categories>
        <category>tracing</category>
      </categories>
      <tags>
        <tag>nodejs</tag>
      </tags>
  </entry>
  <entry>
    <title>2019年回顾</title>
    <url>/2020/01/03/annual-review/</url>
    <content><![CDATA[<p>2019年，虽然有很多不满意，但总的基调是好的。</p>
<span id="more"></span>

<p>学习了诸多技术，包括TypeScript、Cocos、Haskell、OS、Akka和Vue。</p>
<p>Cocos就是个天坑，写这个，幸好从项目上下来了。另一位同事就没那么好运气了，直到现在还在写。</p>
<p>Vue作为后端开发的前端入门框架还是不错的，简单粗暴，出活快，但不够优雅。写前端实在不能称得上是什么有趣的事，光是调CSS一项就足够我崩溃了。花了那么多时间，就只为完成一个小小的样式，感觉并没有学到多少。写完Vue后，坚定了我不搞前端的决心。</p>
<p>TypeScript是在开发Cocos中学习的语言，我简直爱死它了。强大的类型系统，模板元编程，工程友好，完美地符合我的要求。当然，如果不是用开发Cocos就更好了。在其他项目上也见识了有人用不用strict模式，any满天飞，看得我简直想打人了。</p>
<p>Haskell学了一点，还没到Monad，但足以体会到其类型系统的强大，Scala相比之下感觉像个残次品。</p>
<p>补了一点OS的知识。</p>
<p>Akka是年末这几天才开始接触的，主要是新项目要用到。感觉Actor的理解真的很先进，但使用起来也很复杂，还不是很明白，新年继续学习。</p>
<p>工作上顺利成为正式员工。这一年在四个项目上待过，现在回头一看这数量还真不少。</p>
<p>现在回头看第一个项目还是比较简单的。在某种程度上帮助自己很平滑地适应了职场生活，是个好的开始。因为项目小，学了不少技术栈，扩展了视野，也锻炼了ops技能，总体而言，是个好的开始。后来在武汉过的也不错。年末时，抓住机会，成功进入目前的组，开始搞akka/reactor。</p>
<p>生活上，终于走上正轨了，Nice！</p>
<p>掌声送给自己，过去一年确实不容易。新年计划什么也没有，因为去年的计划根本就没看。</p>
]]></content>
      <categories>
        <category>review</category>
      </categories>
  </entry>
  <entry>
    <title>AWS SQS如何实现指数退避</title>
    <url>/2020/08/08/exponential-backoff-in-sqs/</url>
    <content><![CDATA[<h1 id="什么是-SQS？"><a href="#什么是-SQS？" class="headerlink" title="什么是 SQS？"></a>什么是 SQS？</h1><p>SQS 全称 Simple queue service，是 AWS 推出一款消息队列服务。按照 AWS 官方文档的说法，SQS 居有高吞吐、高可用的特性。从我个人的开发体验来看，SQS 是一款相当易用并且功能强大的消息队列服务，满足了生产使用的大部分需求。</p>
<span id="more"></span>

<blockquote>
<p>Amazon Simple Queue Service (SQS) 是一种完全托管的消息队列服务，可让您分离和扩展微服务、分布式系统和无服务器应用程序。SQS 消除了与管理和运营消息型中间件相关的复杂性和开销，并使开发人员能够专注于重要工作。借助 SQS，您可以在软件组件之间发送、存储和接收任何规模的消息，而不会丢失消息，并且无需其他服务即可保持可用。使用 AWS 控制台、命令行界面或您选择的 SDK 和三个简单的命令，在几分钟内即可开始使用 SQS。</p>
</blockquote>
<h1 id="什么是指数退避？"><a href="#什么是指数退避？" class="headerlink" title="什么是指数退避？"></a>什么是指数退避？</h1><p>不过有一项功能 SQS 并没有原生实现，那就是“指数退避”。指数退避是指在消息处理失败时，消息队列会发送消息，直到消息处理成功或者超时。而针对每条消息，每次重试的间隔时间是指数级上升的，比如第一次重试 3 秒，第二次重试 9 秒，第三次重试 27 秒等等，以此类推。这个功能在系统设计中属于常见功能。</p>
<p>虽然 SQS 没有原生实现，但这我们可以利用 SQS 现有 API 来实现。本文关注使用的，就是 SQS 中 VisibilityTimeout 和 ApproximateReceiveCount 属性以<code>changeMessageVisibility</code>API。</p>
<h1 id="什么是-VisibilityTimeout"><a href="#什么是-VisibilityTimeout" class="headerlink" title="什么是 VisibilityTimeout"></a>什么是 VisibilityTimeout</h1><p>VisibilityTimeout 这个概念源于 SQS 中的一个巧妙设计。</p>
<p>当一个消费者接收到 SQS 中的消息后，SQS 并不会立刻将这条消息从队列中删除，因为 SQS 并不知道消费者是否成功接受并处理了消息。如果想要删除这条消息，消费者必须在处理完消息后，手动调用 SQS 的<code>deleteMessage</code>API 去删除，这样 SQS 才会认为这条消费已经被成功消费，可以从队列中删除了。</p>
<p>可问题来了，如果消费者 A 接受到消息后，消息还在队列中，消费者 B 能不能同时接受并处理这条消息呢？如果经常有多个消费者同时处理同一个消息，系统不是就乱套了吗？</p>
<p>基于这种考虑，SQS 对每条消息设置了一个 VisibilityTimeout 的属性，这个属性的值代表了时间长度。如果消费者 A 获得了消息，那么这条消息在 VisibilityTimeout 这么长的时间内是不会被其他消费者读取的，只有超过了这个时间长度，其他消费者才可以读取。也正是基于这个设计，大家在配置 VisibilityTimeout 时长时，要比处理这条消息所花的时间稍长一点。</p>
<p><img src="/images/sqs-visibility-timeout-diagram.png"></p>
<p>有了这个属性，还有操作它的<code>changeMessageVisibility</code>API，我们就可以构思出指数退避的大致方案了。每当消息处理失败时，就重新设置消息的 VisibilityTimeout，并且值是上一次的 N 倍（下面全部都设定为<code>2</code>）。伪代码如下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> message = receiveMessage();<br><span class="hljs-keyword">try</span> &#123;<br>  process(message);<br>&#125; <span class="hljs-keyword">catch</span> (e) &#123;<br>  <span class="hljs-keyword">const</span> retriesCount = getRetriesCount(message);<br>  changeMessageVisibility(message, <span class="hljs-number">2</span> ^ retriesCount);<br>&#125;<br></code></pre></td></tr></table></figure>

<h1 id="什么是-ApproximateReceiveCount？"><a href="#什么是-ApproximateReceiveCount？" class="headerlink" title="什么是 ApproximateReceiveCount？"></a>什么是 ApproximateReceiveCount？</h1><p>上面伪代码需要从消息中获取重试次数，如何获得呢？我们可以在从 SQS 接受消息时，指定额外的<code>Attributes</code>字段告知 SQS 返回相关属性。</p>
<p>请求</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><code class="hljs awk">https:<span class="hljs-regexp">//</span>sqs.us-east-<span class="hljs-number">2</span>.amazonaws.com<span class="hljs-regexp">/xxxxxxxxxxx/</span>MyQueue/<br>?Action=ReceiveMessage<br>&amp;AttributeName=All<br></code></pre></td></tr></table></figure>

<p>响应</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">ReceiveMessageResponse</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">ReceiveMessageResult</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">Message</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">MessageId</span>&gt;</span>5fea7756-0ea4-451a-a703-a558b933e274<span class="hljs-tag">&lt;/<span class="hljs-name">MessageId</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">ReceiptHandle</span>&gt;</span><br>        MbZj6wDWli+JvwwJaBV+3dcjk2YW2vA3+STFFljTM8tJJg6HRG6PYSasuWXPJB+Cw<br>        Lj1FjgXUv1uSj1gUPAWV66FU/WeR4mq2OKpEGYWbnLmpRCJVAyeMjeU5ZBdtcQ+QE<br>        auMZc8ZRv37sIW2iJKq3M9MFx1YvV11A2x/KSbkJ0=<br>      <span class="hljs-tag">&lt;/<span class="hljs-name">ReceiptHandle</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">MD5OfBody</span>&gt;</span>fafb00f5732ab283681e124bf8747ed1<span class="hljs-tag">&lt;/<span class="hljs-name">MD5OfBody</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">Body</span>&gt;</span>This is a test message<span class="hljs-tag">&lt;/<span class="hljs-name">Body</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">Attribute</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">Name</span>&gt;</span>SenderId<span class="hljs-tag">&lt;/<span class="hljs-name">Name</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">Value</span>&gt;</span>195004372649<span class="hljs-tag">&lt;/<span class="hljs-name">Value</span>&gt;</span><br>      <span class="hljs-tag">&lt;/<span class="hljs-name">Attribute</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">Attribute</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">Name</span>&gt;</span>SentTimestamp<span class="hljs-tag">&lt;/<span class="hljs-name">Name</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">Value</span>&gt;</span>1238099229000<span class="hljs-tag">&lt;/<span class="hljs-name">Value</span>&gt;</span><br>      <span class="hljs-tag">&lt;/<span class="hljs-name">Attribute</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">Attribute</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">Name</span>&gt;</span>ApproximateReceiveCount<span class="hljs-tag">&lt;/<span class="hljs-name">Name</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">Value</span>&gt;</span>5<span class="hljs-tag">&lt;/<span class="hljs-name">Value</span>&gt;</span><br>      <span class="hljs-tag">&lt;/<span class="hljs-name">Attribute</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">Attribute</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">Name</span>&gt;</span>ApproximateFirstReceiveTimestamp<span class="hljs-tag">&lt;/<span class="hljs-name">Name</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">Value</span>&gt;</span>1250700979248<span class="hljs-tag">&lt;/<span class="hljs-name">Value</span>&gt;</span><br>      <span class="hljs-tag">&lt;/<span class="hljs-name">Attribute</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">Message</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">ReceiveMessageResult</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">ResponseMetadata</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">RequestId</span>&gt;</span>b6633655-283d-45b4-aee4-4e84e0ae6afa<span class="hljs-tag">&lt;/<span class="hljs-name">RequestId</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">ResponseMetadata</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">ReceiveMessageResponse</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>返回的响应即含有<code>ApproximateReceiveCount</code>，它就是伪代码中的<code>retriesCount</code>。</p>
<h1 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h1><p>本文基于 SQS 已有的 API，构建出一种简单的指数退避方案。除了已提到的内容，还要注意系统的幂等性，避免多次处理消息失败产生额外的影响。</p>
<h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><ul>
<li><a href="https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html">https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html</a></li>
<li><a href="https://docs.aws.amazon.com/AWSSimpleQueueService/latest/APIReference/API_ReceiveMessage.html">https://docs.aws.amazon.com/AWSSimpleQueueService/latest/APIReference/API_ReceiveMessage.html</a></li>
</ul>
]]></content>
      <categories>
        <category>aws</category>
      </categories>
      <tags>
        <tag>nodejs</tag>
      </tags>
  </entry>
  <entry>
    <title>使用haproxy，nginx与aria2搭建下载服务</title>
    <url>/2020/12/26/haproxy-nginx-and-aria2/</url>
    <content><![CDATA[<p>最近搞了一台vps，用它搭建了一个远程下载服务（网盘），包括aria2做下载工具，nginx做静态文件服务，haproxy根据hostname做代理。架构大致如下。</p>
<p><img src="/images/wangpan-arch.jpg" alt="wangpan-arch"></p>
<span id="more"></span>

<p>Aria2配置如下，大家基本可以直接照抄，只需要修改下<code>rpc-secret</code>，用<code>aria2c --conf-path=/root/aria2/aria2.conf -D</code>命令启动即可。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs conf"># touch /data/aria2.session<br># vim /etc/aria2/aria2.conf<br>## &#x27;#&#x27;开头为注释内容, 选项都有相应的注释说明, 根据需要修改 ##<br>## 被注释的选项填写的是默认值, 建议在需要修改时再取消注释  ##<br><br>## 文件保存相关 ##<br><br># 文件的保存路径(可使用绝对路径或相对路径), 默认: 当前启动位置<br>dir=/root/Downloads<br><br># 启用磁盘缓存, 0为禁用缓存, 需1.16以上版本, 默认:16M<br>disk-cache=32M<br># 文件预分配方式, 能有效降低磁盘碎片, 默认:prealloc<br># 预分配所需时间: none &lt; falloc ? trunc &lt; prealloc<br># falloc和trunc则需要文件系统和内核支持<br># NTFS建议使用falloc, EXT3/4建议trunc, MAC 下需要注释此项<br>file-allocation=trunc<br># 断点续传<br>continue=true<br><br>## 下载连接相关 ##<br><br># 最大同时下载任务数, 运行时可修改, 默认:5<br>max-concurrent-downloads=10<br># 同一服务器连接数, 添加时可指定, 默认:1<br>max-connection-per-server=10<br># 最小文件分片大小, 添加时可指定, 取值范围1M -1024M, 默认:20M<br># 假定size=10M, 文件为20MiB 则使用两个来源下载; 文件为15MiB 则使用一个来源下载<br>min-split-size=10M<br># 单个任务最大线程数, 添加时可指定, 默认:5<br>split=5<br># 整体下载速度限制, 运行时可修改, 默认:0<br>#max-overall-download-limit=0<br># 单个任务下载速度限制, 默认:0<br>#max-download-limit=0<br># 整体上传速度限制, 运行时可修改, 默认:0<br>#max-overall-upload-limit=0<br># 单个任务上传速度限制, 默认:0<br>#max-upload-limit=0<br># 禁用IPv6, 默认:false<br>disable-ipv6=true<br><br>## 进度保存相关 ##<br><br># 从会话文件中读取下载任务<br>input-file=/root/aria2/aria2.session<br># 在Aria2退出时保存`错误/未完成`的下载任务到会话文件<br>save-session=/root/aria2/aria2.session<br># 定时保存会话, 0为退出时才保存, 需1.16.1以上版本, 默认:0<br>save-session-interval=60<br><br>## RPC相关设置 ##<br><br>pause=false<br>rpc-allow-origin-all=true<br>rpc-listen-all=true<br>rpc-save-upload-metadata=true<br>rpc-secure=false<br><br># 启用RPC, 默认:false<br>enable-rpc=true<br># 允许所有来源, 默认:false<br>#rpc-allow-origin-all=true<br># 允许非外部访问, 默认:false<br>#rpc-listen-all=true<br># 事件轮询方式, 取值:[epoll, kqueue, port, poll, select], 不同系统默认值不同<br>event-poll=epoll<br># RPC监听端口, 端口被占用时可以修改, 默认:6800<br>rpc-listen-port=6800<br># 设置的RPC授权令牌, v1.18.4新增功能, 取代 --rpc-user 和 --rpc-passwd 选项<br>rpc-secret=XXXXXX<br># 设置的RPC访问用户名, 此选项新版已废弃, 建议改用 --rpc-secret 选项<br>#rpc-user=&lt;USER&gt;<br># 设置的RPC访问密码, 此选项新版已废弃, 建议改用 --rpc-secret 选项<br>#rpc-passwd=&lt;PASSWD&gt;<br><br>## BT/PT下载相关 ##<br><br># 当下载的是一个种子(以.torrent结尾)时, 自动开始BT任务, 默认:true<br>#follow-torrent=true<br># BT监听端口, 当端口被屏蔽时使用, 默认:6881-6999<br>listen-port=51413<br># 单个种子最大连接数, 默认:55<br>#bt-max-peers=55<br># 打开DHT功能, PT需要禁用, 默认:true<br>enable-dht=true<br># 打开IPv6 DHT功能, PT需要禁用<br>#enable-dht6=false<br># DHT网络监听端口, 默认:6881-6999<br>#dht-listen-port=6881-6999<br># 本地节点查找, PT需要禁用, 默认:false<br>bt-enable-lpd=true<br># 种子交换, PT需要禁用, 默认:true<br>enable-peer-exchange=false<br># 每个种子限速, 对少种的PT很有用, 默认:50K<br>#bt-request-peer-speed-limit=50K<br># 客户端伪装, PT需要<br>#peer-id-prefix=-TR2770-<br>user-agent=Transmission/2.92<br>#user-agent=netdisk;4.4.0.6;PC;PC-Windows;6.2.9200;WindowsBaiduYunGuanJia<br># 当种子的分享率达到这个数时, 自动停止做种, 0为一直做种, 默认:1.0<br><br>seed-ratio=1.0<br>#作种时间大于30分钟，则停止作种<br>seed-time=30<br># 强制保存会话, 话即使任务已经完成, 默认:false<br># 较新的版本开启后会在任务完成后依然保留.aria2文件<br>#force-save=false<br># BT校验相关, 默认:true<br>#bt-hash-check-seed=true<br># 继续之前的BT任务时, 无需再次校验, 默认:false<br>bt-seed-unverified=true<br># 保存磁力链接元数据为种子文件(.torrent文件), 默认:false<br>bt-save-metadata=true<br>#下载完成后删除.ara2的同名文件<br>on-download-complete=/root/aria2/delete_aria2<br></code></pre></td></tr></table></figure>

<p>Aria2后端搭建好后，还需要一个前端页面去操作，这里选用<a href="https://github.com/mayswind/AriaNg">AriaNg</a>，下载最新版本，置于<code>/root/Aria2Ng</code>目录下，同时配置nginx来host这个页面。Nginx配置如下。注意，使用了8081端口，之后还会提到它。</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><code class="hljs nginx"><span class="hljs-section">server</span> &#123;<br>    <span class="hljs-attribute">listen</span> <span class="hljs-number">8081</span>;<br><br>    <span class="hljs-attribute">location</span> / &#123;<br>        <span class="hljs-attribute">autoindex</span> <span class="hljs-literal">on</span>;<br>        <span class="hljs-attribute">root</span> /root/AriaNg;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>接着搭建静态文件服务器，在之前的aria2配置里，默认下载目录为<code>/root/Downloads</code>，我们只需要建一个文件下载服务器，绑定到这个目录即可。好在nginx提供了开箱即用的文件下载服务器。配置如下，端口为8080。</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><code class="hljs nginx"><span class="hljs-section">server</span> &#123;<br>    <span class="hljs-attribute">listen</span> <span class="hljs-number">8080</span>;<br><br>    <span class="hljs-attribute">location</span> / &#123;<br>        <span class="hljs-attribute">autoindex</span> <span class="hljs-literal">on</span>;<br>        <span class="hljs-attribute">root</span> /root/Downloads;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这三个服务全都在同一台机器上，使用端口访问很不方便，所以加一个代理，根据hostname的不同代理到不同服务上。这里我用的是haproxy，haproxy是一一款老牌的proxy应用，易用又强大。核心配置如下。</p>
<figure class="highlight apache"><table><tr><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">frontend</span> front<span class="hljs-number">443</span><br>    <span class="hljs-attribute">bind</span> :<span class="hljs-number">443</span> ssl crt-list /etc/haproxy/crt-list.txt alpn http/<span class="hljs-number">1</span>.<span class="hljs-number">1</span><br>    <span class="hljs-attribute">bind</span> :<span class="hljs-number">80</span><br>    <span class="hljs-attribute">mode</span> http<br>    <span class="hljs-attribute">redirect</span> scheme https code <span class="hljs-number">301</span> if !&#123; ssl_fc &#125;<br>    <span class="hljs-attribute">acl</span> is_xswl hdr(host) -i xswl.tnb.tw<br>    <span class="hljs-attribute">acl</span> is_yysy hdr(host) -i yysy.tnb.tw<br>    <span class="hljs-attribute">acl</span> is_awsl hdr(host) -i awsl.tnb.tw<br>    <span class="hljs-attribute">use_backend</span> fileserver<span class="hljs-number">8080</span> if is_xswl<br>    <span class="hljs-attribute">use_backend</span> aria<span class="hljs-number">2</span>c<span class="hljs-number">6800</span> if is_yysy<br>    <span class="hljs-attribute">use_backend</span> AriaNg<span class="hljs-number">8081</span> if is_awsl<br><br><span class="hljs-attribute">backend</span> fileserver<span class="hljs-number">8080</span><br>    <span class="hljs-attribute">mode</span> http<br>    <span class="hljs-attribute">server</span> fileserver<span class="hljs-number">8080</span> <span class="hljs-number">127.0.0.1:8080</span><br><br><span class="hljs-attribute">backend</span> AriaNg<span class="hljs-number">8081</span><br>    <span class="hljs-attribute">mode</span> http<br>    <span class="hljs-attribute">server</span> AriaNg<span class="hljs-number">8081</span> <span class="hljs-number">127.0.0.1:8081</span><br><br><span class="hljs-attribute">backend</span> aria<span class="hljs-number">2</span>c<span class="hljs-number">6800</span><br>    <span class="hljs-attribute">mode</span> http<br>    <span class="hljs-attribute">server</span> aria<span class="hljs-number">2</span>c<span class="hljs-number">6800</span> <span class="hljs-number">127.0.0.1:6800</span><br></code></pre></td></tr></table></figure>

<p>通过acl创建hostname的判断条件，如果符合给定的条件则代理到对应的后端服务上。以下是aria2 frontend与文件下载页面的截图。</p>
<p><img src="/images/aria2ng.png" alt="aria2ng"></p>
<p><img src="/images/nginx-autoindex.png" alt="nginx-autoindex"></p>
]]></content>
      <categories>
        <category>aria2</category>
      </categories>
      <tags>
        <tag>nginx</tag>
        <tag>haproxy</tag>
      </tags>
  </entry>
  <entry>
    <title>我所理解的极简生活</title>
    <url>/2020/12/17/minimalism-life-style/</url>
    <content><![CDATA[<p>我所理解的极简生活，借用奥卡姆剃刀来描述，那就是“如无必要，勿增实体”。这既指精神层面，也指物质层面。</p>
<span id="more"></span>

<p>物质层面很好好理解。</p>
<ol>
<li><p>不购买不需要的物品。</p>
</li>
<li><p>不购买超过自己财力的物品。</p>
</li>
<li><p>定期整理自己的物品，将不需要的卖掉、送人或者丢掉。</p>
</li>
</ol>
<p>下面讲讲精神层面。</p>
<ol>
<li>避免无效社交。这个非常浪费时间。</li>
</ol>
<p><em>未完待续…</em></p>
]]></content>
      <categories>
        <category>生活</category>
      </categories>
  </entry>
  <entry>
    <title>2020年回顾</title>
    <url>/2021/01/03/2020-review/</url>
    <content><![CDATA[<p>2020年是我人生中最不寻常的一年，经历了疫情、换工作两件大事。</p>
<span id="more"></span>

<h2 id="疫情"><a href="#疫情" class="headerlink" title="疫情"></a>疫情</h2><p>这个大家都经历过，不说了。</p>
<h2 id="换工作"><a href="#换工作" class="headerlink" title="换工作"></a>换工作</h2><p>马云有句话说的好，“员工的离职原因很多，但其实就两点：一，钱没给到位；二，心委屈了”。我就属于钱没给到位，心还委屈了的。还好拿到了一份还不错的offer。和新同事相处了一段时间，感觉他们人都还不错。</p>
<h2 id="技术"><a href="#技术" class="headerlink" title="技术"></a>技术</h2><p>上半年主要在学AWS，因为客户在用。我其实是不太想学这个的，因为海外客户，他们的这套技术栈和国内主流技术栈大相迳庭，以后很难用到。不过硬着头皮学习后发现AWS还是很先进的。</p>
<p>下半年主要学习找工作需要的国内常见的技术，包括MySQL, K8s，JVM之类的。因为新工作的关系，对学习了Flink。Flink强大又复杂，学习起来很有意思。</p>
<h2 id="生活"><a href="#生活" class="headerlink" title="生活"></a>生活</h2><p>因为新工作的原因，一个人来到陌生的城市工作。好在之前有很多在网上认识的朋友，给予了一些生活上的指导。和其中几个面基了。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>总的来说还不错，比我预想的要好很多。新的一年也要加油啊！</p>
]]></content>
      <categories>
        <category>review</category>
      </categories>
  </entry>
  <entry>
    <title>使用hammerspoon管理macOS窗口</title>
    <url>/2021/05/26/use-hammerspoon-to-manage-macos-window/</url>
    <content><![CDATA[<p>macOS没有内置窗口管理功能，需要安装第三方软件来实现。常用的免费软件有Spectacle和ShiftIt，这已不再维护。今天将macOS升级到11.3后，发现ShiftIt彻底不能使用了。搜寻后在<a href="https://github.com/fikovnik/ShiftIt/wiki/The-Hammerspoon-Alternative">ShiftIt wiki</a>里找到了替代品<a href="http://www.hammerspoon.org/">hammerspoon</a>。</p>
<span id="more"></span>

<h1 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h1><blockquote>
<p>Hammerspoon 是一款macOS平台的免费开源软件，通过桥接操作系统与 Lua 脚本引擎的方式，让我们可以通过编写 Lua 代码来实现操作应用程序、窗口、鼠标、文本、音频设备、电池、屏幕、剪切板、定位、wifi等。基本囊括了系统的各方面。</p>
</blockquote>
<h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><figure class="highlight mipsasm"><table><tr><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-keyword">brew </span>cask <span class="hljs-keyword">install </span>hammerspoon<br></code></pre></td></tr></table></figure>

<h1 id="编写脚本"><a href="#编写脚本" class="headerlink" title="编写脚本"></a>编写脚本</h1><p>创建<code>.hammerspoon</code>目录和<code>init.lua</code>文件。</p>
<figure class="highlight jboss-cli"><table><tr><td class="code"><pre><code class="hljs jboss-cli">mkdir ~<span class="hljs-string">/.hammerspoon</span><br><span class="hljs-keyword">cd</span> ~<span class="hljs-string">/.hammerspoon</span><br>touch init.lua<br></code></pre></td></tr></table></figure>

<p>编辑<code>init.lua</code>，填写以下内容。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><code class="hljs lua">hs.window.animationDuration = <span class="hljs-number">0</span><br>units = &#123;<br>  right50       = &#123; x = <span class="hljs-number">0.50</span>, y = <span class="hljs-number">0.00</span>, w = <span class="hljs-number">0.50</span>, h = <span class="hljs-number">1.00</span> &#125;,<br>  left50        = &#123; x = <span class="hljs-number">0.00</span>, y = <span class="hljs-number">0.00</span>, w = <span class="hljs-number">0.50</span>, h = <span class="hljs-number">1.00</span> &#125;,<br>  top50         = &#123; x = <span class="hljs-number">0.00</span>, y = <span class="hljs-number">0.00</span>, w = <span class="hljs-number">1.00</span>, h = <span class="hljs-number">0.50</span> &#125;,<br>  bot50         = &#123; x = <span class="hljs-number">0.00</span>, y = <span class="hljs-number">0.50</span>, w = <span class="hljs-number">1.00</span>, h = <span class="hljs-number">0.50</span> &#125;,<br>  maximum       = &#123; x = <span class="hljs-number">0.00</span>, y = <span class="hljs-number">0.00</span>, w = <span class="hljs-number">1.00</span>, h = <span class="hljs-number">1.00</span> &#125;<br>&#125;<br><br>hs.hotkey.bind(&#123; <span class="hljs-string">&#x27;ctrl&#x27;</span>, <span class="hljs-string">&#x27;alt&#x27;</span>, <span class="hljs-string">&#x27;cmd&#x27;</span>&#125;, <span class="hljs-string">&#x27;n&#x27;</span>, <span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">()</span></span><br>  <span class="hljs-keyword">local</span> win = hs.window.focusedWindow()<br>  <span class="hljs-comment">-- get the screen where the focused window is displayed, a.k.a. current screen</span><br>  <span class="hljs-keyword">local</span> screen = win:screen()<br>  <span class="hljs-comment">-- compute the unitRect of the focused window relative to the current screen</span><br>  <span class="hljs-comment">-- and move the window to the next screen setting the same unitRect</span><br>  win:move(win:frame():toUnitRect(screen:frame()), screen:<span class="hljs-built_in">next</span>(), <span class="hljs-literal">true</span>, <span class="hljs-number">0</span>)<br><span class="hljs-keyword">end</span>)<br><br>mash = &#123; <span class="hljs-string">&#x27;ctrl&#x27;</span>, <span class="hljs-string">&#x27;alt&#x27;</span>, <span class="hljs-string">&#x27;cmd&#x27;</span> &#125;<br>hs.hotkey.bind(mash, <span class="hljs-string">&#x27;right&#x27;</span>, <span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">()</span></span> hs.window.focusedWindow():move(units.right50, <span class="hljs-literal">nil</span>, <span class="hljs-literal">true</span>) <span class="hljs-keyword">end</span>)<br>hs.hotkey.bind(mash, <span class="hljs-string">&#x27;left&#x27;</span>, <span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">()</span></span> hs.window.focusedWindow():move(units.left50, <span class="hljs-literal">nil</span>, <span class="hljs-literal">true</span>) <span class="hljs-keyword">end</span>)<br>hs.hotkey.bind(mash, <span class="hljs-string">&#x27;up&#x27;</span>, <span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">()</span></span> hs.window.focusedWindow():move(units.top50, <span class="hljs-literal">nil</span>, <span class="hljs-literal">true</span>) <span class="hljs-keyword">end</span>)<br>hs.hotkey.bind(mash, <span class="hljs-string">&#x27;down&#x27;</span>, <span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">()</span></span> hs.window.focusedWindow():move(units.bot50, <span class="hljs-literal">nil</span>, <span class="hljs-literal">true</span>) <span class="hljs-keyword">end</span>)<br>hs.hotkey.bind(mash, <span class="hljs-string">&#x27;m&#x27;</span>, <span class="hljs-function"><span class="hljs-keyword">function</span><span class="hljs-params">()</span></span> hs.window.focusedWindow():move(units.maximum, <span class="hljs-literal">nil</span>, <span class="hljs-literal">true</span>) <span class="hljs-keyword">end</span>)<br></code></pre></td></tr></table></figure>

<p>这个脚本将若干功能绑定到快捷键上，包括</p>
<ul>
<li><code>ctrl</code>+<code>alt</code>+<code>cmd</code>+<code>n</code> - 将当前窗口移动到下一个显示器</li>
<li><code>ctrl</code>+<code>alt</code>+<code>cmd</code>+<code>↑</code> - 将当前窗口移动到屏幕上半边</li>
<li><code>ctrl</code>+<code>alt</code>+<code>cmd</code>+<code>↓</code> - 将当前窗口移动到屏幕下半边</li>
<li><code>ctrl</code>+<code>alt</code>+<code>cmd</code>+<code>←</code> - 将当前窗口移动到屏幕右半边</li>
<li><code>ctrl</code>+<code>alt</code>+<code>cmd</code>+<code>→</code> - 将当前窗口移动到屏幕左半边</li>
<li><code>ctrl</code>+<code>alt</code>+<code>cmd</code>+<code>m</code> - 将当前窗口最大化</li>
</ul>
<p>配置好后并不能立即使用，还需要启动Hammerspoon。</p>
<h1 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h1><p>打开Hammerspoon软件，点击<code>Reload config</code>，这样就可以愉快地管理窗口了。</p>
<h1 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h1><p>至此，我们已经实现了窗口管理的基本功能。当然，Hammerspoon的功能远不止此，感兴趣的读者可以去<a href="http://www.hammerspoon.org/">Hammerspoon官网</a>了解。</p>
]]></content>
      <categories>
        <category>macos</category>
      </categories>
      <tags>
        <tag>tools</tag>
      </tags>
  </entry>
  <entry>
    <title>我的macOS软件清单</title>
    <url>/2021/06/29/macos-softwares/</url>
    <content><![CDATA[<p>工作后一直用MacBook Pro(15款和17款)，除了公司发的，自己也有一台，现在已经完全习惯了在macOS下开发。macOS下的各种工具基本都把玩了一遍，有一些非常顺手，格外喜欢，这里分享给大家。</p>
<span id="more"></span>

<h1 id="操作系统"><a href="#操作系统" class="headerlink" title="操作系统"></a>操作系统</h1><p>已经升级到了Big Sur，但发现很多软件有兼容性问题，所以还是推荐大家用Catalina。</p>
<ul>
<li>Catalina</li>
</ul>
<h1 id="浏览器"><a href="#浏览器" class="headerlink" title="浏览器"></a>浏览器</h1><p>虽然我很喜欢Firefox，但这些年的发展实在不怎么亮眼。而且Firefox还搞了中国特供版，账号系统和国际版不能兼容，登录时经常不知道到底使用了哪个版本，实在太窝囊了。还是老老实实用Chrome。</p>
<ul>
<li>Chrome</li>
</ul>
<h1 id="编辑器"><a href="#编辑器" class="headerlink" title="编辑器"></a>编辑器</h1><p>已经2021年了，编辑器首选vscode。虽然Sublime Text出了3.x，但用着不如vscode顺手。命令行里一般用vim。学过emacs，但不是很熟练。</p>
<ul>
<li>Visual studio code</li>
<li>Vim</li>
</ul>
<h1 id="IDE"><a href="#IDE" class="headerlink" title="IDE"></a>IDE</h1><p>不用想，写Java/Scala永远离不开Jetbrains家的Intellij IDEA。</p>
<ul>
<li>Intellij IDEA</li>
</ul>
<h1 id="容器"><a href="#容器" class="headerlink" title="容器"></a>容器</h1><p>经常需要启动一些后端中间件，Docker必须有。</p>
<ul>
<li>docker</li>
</ul>
<h1 id="终端"><a href="#终端" class="headerlink" title="终端"></a>终端</h1><p>只有它了。</p>
<ul>
<li>iterm2</li>
</ul>
<h1 id="命令行"><a href="#命令行" class="headerlink" title="命令行"></a>命令行</h1><p>我这几年积累了很多命令行工具，每一个都无可替代。</p>
<ul>
<li>ohmyzsh</li>
<li>ohmyzsh theme<ul>
<li>powerlevel10k <strong>强烈推荐</strong>，功能很完备一个主题</li>
</ul>
</li>
<li>ohmyzsh plugins<ul>
<li>zsh-autosuggestions 根据历史记录自动推荐命令</li>
<li>zsh-syntax-highlighting 命令行语法高亮，再也不用担心用错命令</li>
<li>fzf</li>
<li>git-open</li>
<li>docker</li>
</ul>
</li>
<li>fzf: 快速查找文件、进程</li>
<li>tmux: 终端复用工具，强烈推荐，搭配这个<a href="https://github.com/gpakosz/.tmux">配置</a></li>
<li>tldr: 总结了各个命令的常见用法</li>
<li>nvm: 管理nodejs版本</li>
<li>jenv: 管理jdk版本</li>
<li>z: 智能目录跳转</li>
<li>tig: git客户端</li>
<li>tree: 目录树</li>
<li>ack: grep替代品</li>
<li>mosh: mosh客户端</li>
<li>ncdu: 查看磁盘使用情况</li>
<li>htop: 查看进程</li>
<li>diff-so-fancy: 很好的文本比对工具</li>
</ul>
<h1 id="字体"><a href="#字体" class="headerlink" title="字体"></a>字体</h1><p>更纱黑体作为等宽字体，不仅适配了多国语言，也同时拥有命令行(Term)和等宽(Mono)这两种不同场景的等宽字体。</p>
<ul>
<li><a href="https://github.com/be5invis/Sarasa-Gothic/">更纱黑体 Sarasa Gothic</a></li>
</ul>
<h1 id="窗口管理"><a href="#窗口管理" class="headerlink" title="窗口管理"></a>窗口管理</h1><p>macOS的原生窗口管理功能很少，必须用其他软件加强。rectangle是一个，hammerspoon自己写配置也是一个，其他的都要付费，不是很感冒。</p>
<ul>
<li>Rectangle</li>
</ul>
<h1 id="输入法"><a href="#输入法" class="headerlink" title="输入法"></a>输入法</h1><p>我喜欢五笔，所以并没有太多选择。</p>
<ul>
<li>清歌输入法</li>
</ul>
<h1 id="绘画"><a href="#绘画" class="headerlink" title="绘画"></a>绘画</h1><p>我画的是板绘，可惜windows下流行的板绘软件很多并不支持macOS，最终选择了KDE开源的Krita。</p>
<ul>
<li>Krita</li>
</ul>
<h1 id="键盘化"><a href="#键盘化" class="headerlink" title="键盘化"></a>键盘化</h1><p>键盘的效率远高于鼠标，所以能用键盘完成的事情要尽可能用键盘。</p>
<ul>
<li>alfred4</li>
<li>hammerspoon（可以参考我的上一篇博文）</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这一套配置更侧重于命令行和键盘操作，需要配置的东西也有一些（比如hammerspoon)，但调教好了之后用起来还是很爽的。可惜macOS下好像没什么工具可以很好的操作鼠标，有用过vimac，但发现会提高输入延迟。</p>
]]></content>
      <categories>
        <category>macos</category>
      </categories>
      <tags>
        <tag>tools</tag>
      </tags>
  </entry>
  <entry>
    <title>浅谈TLV协议的实现</title>
    <url>/2021/07/12/tlv-protocol/</url>
    <content><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>众所周知，tcp是一种面向字节流的协议，可以看作一条无尽的水流。如果对水流的内容不加区分，便完全不知道各字节所代表的含义，进而无法处理。如此看来，设计一个能清晰划分字节流边界的应用层协议就显得非常必要。这便涉及到今天所讲的TLV协议。</p>
<span id="more"></span>

<p>TLV全称<a href="https://en.wikipedia.org/wiki/Type%E2%80%93length%E2%80%93value">Type–length–value</a>，是大多数应用层协议的设计思路。主要内容包括</p>
<ol>
<li>最前面的若干位字节表明传统的是否为该协议（本文暂定为4个字节）</li>
<li>紧跟着若干字节（本文暂定为4个字节，也就是Java中的int），代表后续value的字节长度</li>
<li>最后是代表value的字节，其长度为2中所获得的数量</li>
</ol>
<p>整个系统的状态机如图所示。</p>
<p><img src="/images/tlv-state-machine.png" alt="tlv-state-machine"></p>
<h1 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h1><h2 id="不同状态的Handler"><a href="#不同状态的Handler" class="headerlink" title="不同状态的Handler"></a>不同状态的Handler</h2><p>用nio具体编写时，可以先抽象出一个<code>Handler</code>接口，可以接受一个<code>byte</code>进行处理。再添加三个实现（私有方法），代表0、1、2这三种状态对应的<code>Handler</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">Handler</span> </span>&#123;<br>  <span class="hljs-function"><span class="hljs-keyword">int</span> <span class="hljs-title">feed</span><span class="hljs-params">(<span class="hljs-keyword">byte</span> b)</span></span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> <span class="hljs-title">readType</span><span class="hljs-params">(<span class="hljs-keyword">byte</span> b)</span> </span>&#123;<br>    <span class="hljs-comment">// TODO</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> <span class="hljs-title">readLength</span><span class="hljs-params">(<span class="hljs-keyword">byte</span> b)</span> </span>&#123;<br>    <span class="hljs-comment">// TODO</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> <span class="hljs-title">readValue</span><span class="hljs-params">(<span class="hljs-keyword">byte</span> b)</span> </span>&#123;<br>    <span class="hljs-comment">// TODO</span><br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="状态的定义"><a href="#状态的定义" class="headerlink" title="状态的定义"></a>状态的定义</h2><p>接着定义三种状态对应的code和一个<code>Handler</code>数组，这样后面可以用当前状态去取对应的<code>Handler</code>（<code>handlers[state]</code>）。<code>state</code>表示当前状态，初始值当然是<code>TYPE_STATE</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> TYPE_STATE = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> LENGTH_STATE = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">int</span> VALUE_STATE = <span class="hljs-number">2</span>;<br><br><span class="hljs-keyword">final</span> Handler[] handlers = <span class="hljs-keyword">new</span> Handler[]&#123;<br>    <span class="hljs-keyword">this</span>::readType,<br>    <span class="hljs-keyword">this</span>::readLength,<br>    <span class="hljs-keyword">this</span>::readValue<br>&#125;;<br><br><span class="hljs-keyword">int</span> state = TYPE_STATE;<br></code></pre></td></tr></table></figure>

<h2 id="对外接口"><a href="#对外接口" class="headerlink" title="对外接口"></a>对外接口</h2><p>对外接口是<code>read()</code>，供外层在读事件就绪时调用。这里会不停地一个字节一个字节的读取ByteBuffer，并调用当前状态的<code>Handler</code>去处理。如果返回<code>-1</code>代表处理失败，需要关闭连接。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">read</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;<br>  <span class="hljs-keyword">while</span> (<span class="hljs-keyword">true</span>) &#123;<br>    <span class="hljs-keyword">int</span> i = channel.read(buffer);<br>    <span class="hljs-keyword">if</span> (i == -<span class="hljs-number">1</span>) &#123;<br>      close();<br>      <span class="hljs-keyword">return</span>;<br>    &#125;<br>    <span class="hljs-keyword">if</span> (i == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span>;<br><br>    buffer.flip();<br>    <span class="hljs-keyword">while</span> (buffer.hasRemaining()) &#123;<br>      <span class="hljs-keyword">byte</span> b = buffer.get();<br>      <span class="hljs-keyword">int</span> f = handlers[state].feed(b);<br>      <span class="hljs-keyword">if</span> (f == -<span class="hljs-number">1</span>) &#123;<br>        close();<br>        <span class="hljs-keyword">return</span>;<br>      &#125;<br>    &#125;<br>    buffer.clear();<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="接受到的数据"><a href="#接受到的数据" class="headerlink" title="接受到的数据"></a>接受到的数据</h2><p>接着定义若干<code>byte[]</code>，代表每个状态接收的字节，同时还有若干<code>Pointer</code>代表当前填充到第几个字节。注意，代表<code>value</code>的<code>data</code>并没有初始化，因为它的长度是根据<code>length</code>确定的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">final</span> <span class="hljs-keyword">byte</span>[] type = <span class="hljs-keyword">new</span> <span class="hljs-keyword">byte</span>[<span class="hljs-number">4</span>];<br><span class="hljs-keyword">byte</span> typePointer = <span class="hljs-number">0</span>;<br><br><span class="hljs-keyword">final</span> <span class="hljs-keyword">byte</span>[] length = <span class="hljs-keyword">new</span> <span class="hljs-keyword">byte</span>[<span class="hljs-number">4</span>];<br><span class="hljs-keyword">byte</span> lengthPointer = <span class="hljs-number">0</span>;<br><br><span class="hljs-keyword">int</span> dataLength = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">byte</span>[] data;<br></code></pre></td></tr></table></figure>

<h2 id="readType-Handler"><a href="#readType-Handler" class="headerlink" title="readType Handler"></a>readType Handler</h2><p>接下来就可以实现之前提到的三个<code>Handler</code>了。先看<code>readType</code>，每次一次<code>byte</code>进来，它就会填充到<code>type</code>(byte[])的下一位，并判断是否满足长度，如果不满足则等待下一个byte。如果长度够4位了，但内容和约定的不一样，会返回<code>-1</code>，代表内容有误，供上层处理。如果长度和内容正确，那么状态就转移到<code>LENGTH_STATE</code>上，由下一个<code>readLength Handler</code>处理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> <span class="hljs-title">readType</span><span class="hljs-params">(<span class="hljs-keyword">byte</span> b)</span> </span>&#123;<br>  type[typePointer] = b;<br>  <span class="hljs-keyword">if</span> (typePointer != <span class="hljs-number">3</span>) &#123;<br>    typePointer++;<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>  &#125;<br>  typePointer = <span class="hljs-number">0</span>;<br>  <span class="hljs-keyword">if</span> (!Arrays.equals(type, <span class="hljs-keyword">new</span> <span class="hljs-keyword">byte</span>[]&#123;<span class="hljs-number">0x01</span>, <span class="hljs-number">0x23</span>, <span class="hljs-number">0x45</span>, <span class="hljs-number">0x67</span>&#125;)) &#123;<br>    <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;<br>  &#125;<br>  state = LENGTH_STATE;<br>  <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="readLength-Handler"><a href="#readLength-Handler" class="headerlink" title="readLength Handler"></a>readLength Handler</h2><p><code>readLength Handler</code>和上面也是类似的，额外多了一步，将读取的value长度保存到<code>dataLength</code>字段中，并将状态转移到<code>VALUE_STATE</code>上。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> <span class="hljs-title">readLength</span><span class="hljs-params">(<span class="hljs-keyword">byte</span> b)</span> </span>&#123;<br>  length[lengthPointer] = b;<br>  <span class="hljs-keyword">if</span> (lengthPointer != <span class="hljs-number">3</span>) &#123;<br>    lengthPointer++;<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>  &#125;<br>  lengthPointer = <span class="hljs-number">0</span>;<br>  dataLength = ByteBuffer.wrap(length).getInt();<br>  <span class="hljs-keyword">if</span> (dataLength == <span class="hljs-number">0</span>) &#123;<br>    <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;<br>  &#125;<br>  data = <span class="hljs-keyword">new</span> <span class="hljs-keyword">byte</span>[dataLength];<br>  state = VALUE_STATE;<br>  <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="readValue-Handler"><a href="#readValue-Handler" class="headerlink" title="readValue Handler"></a>readValue Handler</h2><p><code>readValue Handler</code>将字节存储起来，直到数量和<code>readLength</code>中获得的<code>dataLength</code>值一样，之后就可以进行正常的业务逻辑处理，我这里是简单地print所有数据。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">int</span> <span class="hljs-title">readValue</span><span class="hljs-params">(<span class="hljs-keyword">byte</span> b)</span> </span>&#123;<br>  data[dataPointer] = b;<br>  <span class="hljs-keyword">if</span> (dataPointer != dataLength - <span class="hljs-number">1</span>) &#123;<br>    dataPointer++;<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>  &#125;<br>  dataPointer = <span class="hljs-number">0</span>;<br>  process();<br>  state = TYPE_STATE;<br>  <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">process</span><span class="hljs-params">()</span> </span>&#123;<br>  <span class="hljs-keyword">if</span> (data == <span class="hljs-keyword">null</span>) <span class="hljs-keyword">return</span>;<br>  String s = <span class="hljs-keyword">new</span> String(data, StandardCharsets.UTF_8);<br>  System.out.println(s);<br>&#125;<br></code></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>tlv协议的实现大致就是这样。实现一个状态机，根据输入，判断是否转移到下一步状态。本文的实现比较简单，<code>value</code>是作为请求的<code>body</code>存在的，如果需要，还可以加上<code>Header</code>或timeout处理之类额外的功能。</p>
<p>原文代码在<a href="https://github.com/gcnyin/raw-nio/blob/master/src/main/java/com/github/gcnyin/rawnio/tlv">这里</a>，里面除了TLV还有其他java nio的实践，欢迎交流。</p>
<p>参考资料</p>
<ul>
<li><a href="https://en.wikipedia.org/wiki/Type%E2%80%93length%E2%80%93value">https://en.wikipedia.org/wiki/Type%E2%80%93length%E2%80%93value</a></li>
</ul>
]]></content>
      <categories>
        <category>network</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
</search>
